code {
  full_code: "import java.util.*;\n\npublic class ComplexCollections {\n    public static void main(String[] args) {\n        Scanner sc = new Scanner(System.in);\n        int n = sc.nextInt();\n        \n        // Use various collections\n        List<Integer> list = new ArrayList<>();\n        Set<Integer> set = new HashSet<>();\n        Map<Integer, Integer> map = new HashMap<>();\n        Queue<Integer> queue = new LinkedList<>();\n        Stack<Integer> stack = new Stack<>();\n        \n        for (int i = 0; i < n; i++) {\n            int val = sc.nextInt();\n            list.add(val);\n            set.add(val);\n            map.put(i, val);\n            queue.offer(val);\n            stack.push(val);\n        }\n        \n        // Sort the list\n        Collections.sort(list);\n        \n        System.out.println(\"List: \" + list);\n        System.out.println(\"Set size: \" + set.size());\n        System.out.println(\"Map keys: \" + map.keySet());\n        System.out.println(\"Queue peek: \" + queue.peek());\n        System.out.println(\"Stack peek: \" + stack.peek());\n        \n        sc.close();\n    }\n}"
  filename: "ComplexCollections.java"
  binary_filename: "ComplexCollections"
}
language: "java"
compilation_options {
  resource_limits {
    memory_limit: 102400
    time_limit: 6
  }
}
runtime_options {
  resource_limits {
    memory_limit: 51200
    time_limit: 5
    process_limit: 300
  }
}
testcases {
  input: "5 3 1 4 1 5"
  output: "List: [1, 1, 3, 4, 5]\nSet size: 4\nMap keys: [0, 1, 2, 3, 4]\nQueue peek: 3\nStack peek: 5\n"
}