code {
  full_code: "public class TimeExceeded {\n    public static void main(String[] args) {\n        // Infinite loop to ensure timeout\n        while (true) {\n            // Keep CPU busy\n            Math.sqrt(Math.random());\n        }\n    }\n}"
  filename: "TimeExceeded.java"
  binary_filename: "TimeExceeded"
}
language: "java"
compilation_options {
  resource_limits {
    memory_limit: 102400
    time_limit: 6
  }
}
runtime_options {
  resource_limits {
    memory_limit: 51200
    time_limit: 2
    process_limit: 300
  }
}
testcases {
  input: ""
  output: "This will never be printed due to timeout\n"
}