code {
  full_code: "# Advanced Python program demonstrating various features\n# This is a positive test case for advanced functionality\n\n# Class definition (Python 2 compatible)\nclass Calculator(object):\n    def __init__(self):\n        self.history = []\n    \n    def add(self, a, b):\n        result = a + b\n        self.history.append(\"Added \" + str(a) + \" + \" + str(b) + \" = \" + str(result))\n        return result\n    \n    def multiply(self, a, b):\n        result = a * b\n        self.history.append(\"Multiplied \" + str(a) + \" * \" + str(b) + \" = \" + str(result))\n        return result\n    \n    def get_history(self):\n        return self.history\n\n# Create calculator instance\ncalc = Calculator()\n\n# Perform calculations\nresult1 = calc.add(10, 5)\nresult2 = calc.multiply(3, 4)\nresult3 = calc.add(result1, result2)\n\nprint(\"Results:\")\nprint(\"First addition: \" + str(result1))\nprint(\"Multiplication: \" + str(result2))\nprint(\"Final addition: \" + str(result3))\n\nprint(\"\\nCalculation History:\")\nfor entry in calc.get_history():\n    print(entry)\n\n# List comprehension\nnumbers = [1, 2, 3, 4, 5]\nsquares = [x * x for x in numbers]\nprint(\"\\nSquares: \" + str(squares))\n\n# Dictionary operations (Python 2 compatible)\nword_lengths = {\"hello\": len(\"hello\"), \"world\": len(\"world\"), \"python\": len(\"python\")}\nprint(\"Word lengths: \" + str(word_lengths))"
}
language: "py"
compilation_options {
  resource_limits {
    memory_limit: 1024
    time_limit: 5
  }
}
testcases {
  input: ""
  output: "Results:\nFirst addition: 15\nMultiplication: 12\nFinal addition: 27\n\nCalculation History:\nAdded 10 + 5 = 15\nMultiplied 3 * 4 = 12\nAdded 15 + 12 = 27\n\nSquares: [1, 4, 9, 16, 25]\nWord lengths: {'python': 6, 'world': 5, 'hello': 5}\n"
}
