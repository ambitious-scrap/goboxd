# Build output
/bin/

# Locally-installed tooling (e.g. hey for benchmarks) — not vendored
/.tools/

# Private AI development journals — kept local, not version-controlled
/docs/ai/

# Personal prep / private notes — kept local, not version-controlled
/PERSONAL_README.md
/docs/INTERVIEW_PREP.md

# Agent tooling artifacts — kept local, not version-controlled
/.codex/
/AGENTS.md

# Local handoff notes — not version-controlled
/handoff.md

# Judging fixtures + OS cruft — kept local
/payloads/
.DS_Store

# Stage 3 Design and Rationale doc (ignored per user request)
/docs/DESIGN_AND_RATIONALE.md

# PR Analysis & Local PR copies
/pr_analysis/
/pull_requests/

# Stage 1 leaderboard (compiled from judge comments) — kept local
/goboxd-stage1-leaderboard.md

# graphify knowledge-graph artifacts — generated, kept local
/graphify-out/

# Claude Code session/tooling artifacts — kept local
/.claude/
/.github/skills/

# Serena tooling — kept local
/.serena/

# Reference implementation + its fixtures — provided locally for the differential
# conformance test (tests/conformance), deliberately not bundled in this repo.
/pyjail/
/pyjail-old/

# Generated go fuzz corpus (seed inputs live in *_test.go via f.Add).
**/testdata/fuzz/

# Local validation/benchmark run logs & reports — generated, kept local
/validation-logs/

# vegeta intermediate reports
docs/loadtest*/report-*.bin
docs/loadtest*/report-*.json
